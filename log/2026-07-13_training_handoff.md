# 2026-07-13 训练与监督交接文档

## 本次工作

- 在项目根目录新增 `TRAINING_HANDOFF.md`。
- 记录当前 IImages 场景专用 YOLOv8n-seg 模型、数据规模、指标和部署文件。
- 明确原始素材、标注、训练、Git 和部署四类红线。
- 固定当前默认训练环境、模型、输入尺寸、batch、epochs、patience、置信度和 ONNX 参数。
- 写入训练前备份、只读数据审计、数据集构建、CUDA 检查、前台/后台训练启动的完整流程。
- 说明 `monitor_iimages_training.ps1` 的参数、每小时检查内容、启动方式和限制。
- 写入训练中暂停条件、训练结束验收、真正 resume 与重新微调的区别。
- 汇总路径空格、CUDA 环境、空 JSON、派生标注、场景泄漏、polars、ONNX 和 GitHub 推送等常见错误。

## 关键决定

- 默认仍要求训练前备份；2026-07-13 那轮不备份、不去重属于一次性明确豁免，不得作为后续默认规则。
- 当前 IImages 模型固定为 YOLOv8n-seg、512 输入、Parking 单类别、四边形训练标注。
- 监督脚本只记录训练状态和 GPU 指标，不自动修改参数、重启训练或向聊天窗口报警。
- 没有同名 JSON 的图片不得作为负样本；有 shapes 但全部过滤的图片也不得伪造成空标签。

## 验证

- UTF-8 中文检查通过。
- 文档必需章节检查通过。
- Markdown 代码块数量检查通过。
- `git diff --check` 通过。
- 单元测试：19 项全部通过。

本次只新增文档和 Markdown 日志，没有修改原始素材、生成数据集、训练权重或部署模型。
